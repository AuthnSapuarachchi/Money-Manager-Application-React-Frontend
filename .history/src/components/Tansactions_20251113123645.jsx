const Transaction = ({transactions, onMore, type}) => {
    return (
        <div className="card">
            <div className="flex items-center justify-between">
                <h5 className="text-lg"> {ti}</h5>
            </div>
        </div>
    )
}

export default Transaction