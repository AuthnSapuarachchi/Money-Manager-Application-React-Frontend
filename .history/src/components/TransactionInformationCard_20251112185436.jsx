const TransactionInformationCard = ({icon, title, date, amount, type, hideDeleteBtn, on}) => {
    return  (
        <div>TransactionInformationCard</div>
    )
}

export default TransactionInformationCard